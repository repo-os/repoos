# Build output (web shell) — regenerated by `bun run build`
www/

# Dependencies
node_modules/

# macOS
.DS_Store

# Symlink to the latest debug APK, maintained by `just build-android`
app-debug.apk

# Native build artifacts are already covered by the generated
# ios/.gitignore and android/.gitignore (android/app/.gitignore).
