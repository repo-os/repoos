# Contributing

1. Pick a task from the board whose status is `ready` (or file a new one).
2. Set it to `active` and create the branch named in its frontmatter.
3. Implement. `repoos check` is the single bar for "did this break anything?"
4. Set `status: review` on a green check and stop — a human signs off before
   anything merges.
