# UI/UX Design Review Report

**Generated**: 2026-08-16T11:39:39.943Z

## Executive Summary

- **Files Scanned**: 67
- **Findings Identified**: 24
- **UI bugs**: 8
- **UX frictions**: 8
- **Design recommendations**: 8

## Scan Overview

- Analyzed 67 files under `src/ui-app/src/` (43 components, 6 views).

## UI Bugs

### Medium Severity

**Hardcoded inline style "background: var(--amber-tint); color: var(--amber)" bypasses the shared design system.**
- File: `components/NeedsYouPanel.vue`:29
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color: var(--cyan)" bypasses the shared design system.**
- File: `components/NeedsYouPanel.vue`:64
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color: var(--cyan)" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:2092
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color: var(--cyan); font-size: 11px" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:2220
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color: #4ef0a8;" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:2681
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color: #ff6b6b;" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:2685
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color:var(--txt-faint)" bypasses the shared design system.**
- File: `stores/repo.ts`:469
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "color:var(--red)" bypasses the shared design system.**
- File: `stores/repo.ts`:1253
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

## UX Friction

### Medium Severity

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `components/NeedsYouPanel.vue`:40
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `components/NeedsYouPanel.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `components/SearchBar.vue`:208
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `components/SearchBar.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `components/SearchBar.vue`:241
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `components/SearchBar.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `components/TaskCard.vue`:221
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `components/TaskCard.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `views/ContextView.vue`:141
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `views/ContextView.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `views/ContextView.vue`:151
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `views/ContextView.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `views/ContextView.vue`:169
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `views/ContextView.vue`.

**A <div> element carries a @click handler but no role="button" or tabindex.**
- File: `views/ProductManagerView.vue`:190
- **Rationale**: Click-only handlers on non-interactive elements are unreachable by keyboard and screen readers don't announce them as actionable.
- **Suggested fix**: Add role="button" and tabindex="0" (plus Enter/Space handling) or use a real <button> in `views/ProductManagerView.vue`.

## Proposed Updates, Fixes, and New Designs

### Medium Severity

**Hardcoded inline style "margin-bottom: 16px" bypasses the shared design system.**
- File: `components/AutoEngineeringPanel.vue`:90
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "flex: 1; min-width: 0" bypasses the shared design system.**
- File: `components/FeedPanel.vue`:46
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "cursor: pointer" bypasses the shared design system.**
- File: `components/NeedsYouPanel.vue`:44
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "flex: 1; min-width: 0" bypasses the shared design system.**
- File: `components/NeedsYouPanel.vue`:52
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "margin-top: 20px" bypasses the shared design system.**
- File: `components/NewDocPanel.vue`:308
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "margin-top: 20px" bypasses the shared design system.**
- File: `components/NewDocPanel.vue`:360
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "margin-top: 4px" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:1633
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

**Hardcoded inline style "margin-bottom:14px" bypasses the shared design system.**
- File: `components/TaskDrawer.vue`:1692
- **Rationale**: Inline styles ignore the centralized CSS variables and can drift from the theme, especially across dark mode.
- **Suggested fix**: Move this styling into a scoped class or a shared utility so it inherits the app's theme tokens (see how neighboring `src/ui-app/src/components/*.vue` components style via CSS variables).

## Next Steps

- Review the findings and confirm each is worth addressing.
- Create follow-up tasks for the agreed-upon fixes/redesigns (this agent reports only; it does not edit UI source).
- Track progress through subsequent reports under `docs/agents/Design/`.
